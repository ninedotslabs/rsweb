# RSWeb
Rust Web Template.

Libs: [warp, ructe]

Development

first, test

```
$ cargo test
```

if test is good and no error, then run

```
$ cargo run
```

save your time by testing first rather than `loop { edit->run }`.
